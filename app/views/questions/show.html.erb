<center>
  <p id="notice"><%= notice %></p>

  <p>
    <strong>ユーザーID:</strong>
    <%= @question.user_code %>
  </p>

  <p>
    <strong>カテゴリ:</strong>
    <%= @question.category %>
  </p>

  <p>
    <strong>タイトル:</strong>
    <%= @question.title %>
  </p>

  <p>
    <strong>内容:</strong>
    <%= @question.contents %>
  </p>

  <div  class="fixed_btn">
    <p><b>コメントを追加する</b></p>
    <%= form_tag('/question_reviews') do %>
      <%= hidden_field :contents, :question_code, :value => @question.id %>
      <p>
        <%= text_field :contents, :contents %>
        <%= submit_tag "　追　　加　" %>
      </p>
    <% end %>
  </div>
</center>

<p><b>ベストアンサー</b></p>

<p><b>コメント</b></p>
<% @question_reviews.each do |q| %>
    <p>
      <%= q.created_at %>
    <br>
      <%= q.contents %>
    <br>
      <% if session[:login_id] == @question.user_code %>
        <%= form_tag('/questions_best_answer') do %>
        　<%= hidden_field :question_reviews, :id, :value => q.id %>
          <%= submit_tag "👍", class: "button_white" %>
        <% end %>
      <% end %>
    </p>
<% end %>

<p><%= button_to 'しつもんひろばへ戻る', '/questions', method: :get %></p>

